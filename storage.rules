rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Files stored under /files/{userId}/{fileId}
    // Only the owner can upload, read, and delete their files
    match /files/{userId}/{fileId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}